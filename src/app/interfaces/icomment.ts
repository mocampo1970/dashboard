// Interface que va a contener unn user que es el usuario, el date que es la fecha en la cual escribio el
// usuario 
export interface IComment {
    user: string,
    date: Date,
    text: string,
    post: string
}
