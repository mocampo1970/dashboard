// Debe tener los mismos atributos de la clase en java en spring boot
// y con los mismos datos, esta se crea como una clase
export class Comment {
    id: number;
    fecha: Date;
    post: number;
    text: string;
    user: string;
}

